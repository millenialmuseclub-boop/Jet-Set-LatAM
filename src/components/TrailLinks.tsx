import { Link } from 'react-router-dom'
import { trails } from '@/data/trails'
export function TrailLinks({destinationId,query=""}:{destinationId?:string;query?:string}) { const list=trails.filter(t=>(!destinationId||t.destinationId===destinationId)&&[t.title,t.description,t.theme].join(" ").normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().includes(query.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase()));if(!list.length)return <p className="my-6 text-sm text-ink-soft">No Trails match these filters yet.</p>;return <section className="my-7"><h2 className="mb-4 font-display text-3xl italic">A way through the city.</h2><div className="grid gap-4 sm:grid-cols-2">{list.map(t=><Link key={t.id} to={'/trails/'+t.id} className="rounded-2xl border border-ink/15 bg-cream p-5"><p className="eyebrow text-terracotta">Jet Set Trails · {t.theme}</p><h3 className="my-2 font-display text-2xl">{t.title}</h3><p className="text-xs text-ink-soft">{t.placeIds.length} stops · about {t.estimatedHours} hours</p></Link>)}</div></section>}
