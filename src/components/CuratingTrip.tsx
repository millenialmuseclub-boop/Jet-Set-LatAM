import { Photo } from './Photo'
import type { Destination } from '@/types'
import { getPlacesByDestination } from '@/data'
import { rio2025 } from '@/data/rio-2025'
export function CuratingTrip({destination}:{destination:Destination}){return <div role="status" aria-live="polite" className="curating-trip mx-auto max-w-lg px-5 py-6"><div className="relative mb-7 h-56 overflow-hidden rounded-2xl"><Photo src={destination.heroPhoto} seed={destination.id} alt="" className="h-full w-full"/>{<div className="curating-photo absolute inset-0"><Photo src={destination.id==='rio-de-janeiro'?rio2025[9].src:getPlacesByDestination(destination.id).find(p=>p.photos.length)?.photos[0]||destination.heroPhoto} seed="rio-curating" alt="" className="h-full w-full"/></div>}<div className="absolute inset-0 bg-gradient-to-t from-ink/45 to-transparent"/></div><p className="eyebrow text-terracotta">A journey, considered</p><h1 className="my-3 font-display text-3xl italic">Curating your {destination.city} trip</h1><div className="curating-rule my-6" aria-hidden="true"/><div className="curating-phrases relative h-12 text-sm text-ink-soft" aria-hidden="true"><p>Finding the places worth your time.</p><p>Leaving room for a little discovery.</p><p>Your days are coming together.</p></div><p className="text-xs text-ink-soft">Built from the Jet Set collection.</p></div>}
